# Assets

Screenshots for every investigation and lab-setup document in this repository are embedded directly via GitHub's `user-attachments` CDN (the images render inline within each Markdown file). This folder is reserved for any future local diagrams, exported architecture images, or supporting files that are not already hosted inline.
